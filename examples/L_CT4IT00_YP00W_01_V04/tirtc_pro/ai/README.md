# AI 对话与联系人呼叫

当前功能与云端配置集中在 [AI 呼叫配置篇](../docs/AI呼叫微信与设备配置详解.md)，操作见 [AI 对话](../docs/当前设备操作.md#ai)。本模块支持普通呼叫默认视频、显式 `audio/voice` 使用语音；最终转交原有 calls/UI 流程。

## 入口与生命周期

- `tirtc_ai_start_service()` 创建 20 KiB、优先级 13 的业务线程；重复启动不创建第二个实例。
- `tirtc_ai_action()` 接受 UI 的开始、停止和导航；0 仅表示受理。
- `tirtc_ai_publish()` 复制 AI 状态和字幕，工作线程自行调用；不要从小栈 GUI 周期调用大快照函数。
- 正式身份、时间、平台 API/MQTT、4G 和 runtime 就绪后才建连。凭据/网络代次变化使原请求失效。
- 获取 AI token、受管 WHIP、发送 `0x2100 start_session`，确认格式后进入对话。后台预备不能占用本应接受 LIVE 的空闲首页。
- 停止后禁止新媒体，发送结束、等待音频和连接安全释放。HOME/CLOCK/MENU/DIAGNOSTICS 可保留 AI；其他页面请求停止。

## 当前音频参数

以下以 `tirtc_ai.c` 常量为准，不沿用早期 120 ms ahead / 960 字节预缓冲参数。

| 项目 | 当前值 |
| --- | --- |
| 音频 | 8 kHz、单声道 PCM16；20 ms = 160 样本 = 320 PCM 字节 = 160 A-law 字节 |
| 上行 stream_id | 1 |
| 接收字节预算 | `AI_RX_BYTES=15360`，8 kHz A-law 约 1.92 秒 |
| 命令槽 | 3 × 4608 字节 |
| 一轮播放提交上限 | 24 块，仍受 20 ms 工作预算/SDK 调用返回约束 |
| 已提交音频领先上限 | 480 ms |
| 起播积累目标 | 1920 A-law 字节，约 240 ms |
| 短句不足阈值的最长等待 | 120 ms |
| 播放轮询 | 5 ms |

采集由独立采集路径取数，硬件一次取得 40 ms PCM，再按 20 ms 交付。不要把 40 ms SDK 等待当作每个 20 ms 编码帧都阻塞一次。

连接期间尽早提交 DAC 静音预热，与网络建连重叠；首个真实播放块保留剩余预热。具体约定见 [audio_device.h](../media/audio_device.h)。不承诺任何网络下都在两秒内回答，也不能直接去掉预热造成首字衰减。

AI 播放期间抑制麦克风上行，并保留尾部保护；这不是经过验证的全双工 AEC。静音或保护时丢弃剩余采集缓存，避免之后上传过期声音。

## AI 呼叫接管

标准命令 `device_action/call_contact` 与有效旧式 `call_intent/ai_call_intent` 进入统一处理：校验 → 请求新联系人刷新票据 → 精确唯一匹配 → 返回受理结果 → 排队 calls → 停止 AI → 取得媒体使用权拨号。

不猜联系人，不因为云端说“正在呼叫”就当作已拨出；显式语音参数不会被默认视频覆盖。取消、来电、断网、解绑使旧请求失效。协议、结果码和示例见主文档，避免在这里重复维护两套配置说明。

## 诊断与限制

常态保留启动和必要错误；周期采播计数受 `TIRTC_ENABLE_DEBUG_LOG` 控制。`[AI-CALL] handoff` 代表已转交通话模块，不代表对端接听。

SDK 的阻塞录音/异步播放退出约束由 media 适配保留，不通过强制删除任务或释放在途缓冲来“快速恢复”。构建、模拟检查和实机效果分别记录，当前包的验证状态见 [发布说明](../../../../release/tirtc_pro/README.md)。
